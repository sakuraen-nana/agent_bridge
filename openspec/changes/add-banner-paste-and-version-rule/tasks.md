# Tasks: add-banner-paste-and-version-rule

> 实施顺序：解析扩展（1）→ 模板与文档（2）→ 版本规则（3）→ 测试（4）→ 实跑（5）→ 收尾（6）。
> 决策依据见 design.md（D1 单解析入口、D2 键别名、D3 显式 RFC1918 判别、D4 空值不覆盖、
> D5 无地址则忽略、D6 版本规则落在 AGENTS.md 且归档时 bump、D7 模板主推粘贴）。
> 文档边界按既有约定：本仓库不记录使用方的机器名、IP 与部署路径，需指代环境处用通用角色。

## 1. 解析扩展（client.py）

- [x] 1.1 `parse_token_doc` 支持横幅行形态：新增键别名（「局域网 ip」→ `host`），并确认 `Token` 行仍映射为 `token`；既有 `key: value` 解析结果 MUST 不变；验证：粘贴式文档能解析出 token 与 host，既有文档用例全绿 —— 证据：提交 `2619db0`；新增 `_KEY_ALIASES = {"局域网 ip": "host"}`，既有 5 项解析用例（字段与空白、注释空行、行内 #、多组后者覆盖、键小写）全部保持通过
- [x] 1.2 新增地址取值：在候选地址中优先 RFC1918（`10/8`、`172.16/12`、`192.168/16`），无则取第一个合法地址；候选为空时该行不产生 host；MUST NOT 使用 `ipaddress.is_private`（判定集合随 Python 版本演进，违背「任意 3.7+ 行为一致」）；验证：用例覆盖 CGNAT 风格地址（`100.x`）被跳过、无私网地址时取第一个、无地址行被忽略 —— 证据：提交 `2619db0`；`_pick_banner_host` 手写区间判别；用例三例分别断言 `100.64.9.9, 192.168.7.21` → 后者、`100.64.9.9, 203.0.113.7` → 前者、整句说明行 → 不产生 host
- [x] 1.3 值为空的行不参与覆盖；验证：把模板式空 `token:` / `host:` 行置于粘贴横幅之后，取值仍来自横幅（用例） —— 证据：用例 `test_empty_value_does_not_override`（横幅后接空 `token:` / `host:` / `updated:` 行，取值仍来自横幅）

## 2. 模板与文档

- [x] 2.1 重写 `bridge.local.md.example` 的填写说明：主推「直接粘贴被控机控制台输出」，保留 `token:` / `host:` / `updated:` 字段供手填或覆盖 host；安全警示不变；验证：按模板的两种填法各走通一次 —— 证据：提交 `8e866c4`；粘贴写法见 §5.1 实跑，手工字段写法由既有客户端用例覆盖
- [x] 2.2 `README.md` 的 token 文档一节同步说明：可直接粘贴横幅、多地址时优先私网、手工 `host:` 行可覆盖；验证：照 README 步骤可复现，守卫 grep（机器名 / IP / 部署路径）零命中 —— 证据：提交 `8e866c4`；守卫 grep 除 LICENSE 作者署名（既有豁免）外零命中；改动文件中的 IP 全为文档保留段（`10.x` / `192.168.1.x` / `192.168.7.21` / `100.64.9.9` / `203.0.113.7`）

## 3. 版本推进规则

- [x] 3.1 `AGENTS.md` 写入版本推进规则：每个变更**归档时**把 `BRIDGE_VERSION` 的 minor 加一（缺陷修复类加 patch），并在开发流程的归档条目中加入「bump 版本」一项；规则 MUST NOT 写死具体数字（在途变更与本变更谁先归档谁取 0.2.0）；验证：照规则可执行且无歧义 —— 证据：提交 `6e68e67`；写入 `AGENTS.md` 开发流程第 6 条，数字表述为「取归档当时的现值加一，不预设不追溯」
- [x] 3.2 复核 `README.md` 与模板是否需提及版本来源（何处定义、何时推进）；验证：需则补齐、不需则记录结论 —— 证据：结论为**无需**——README 面向使用方，版本号已在启动横幅与 `hello` 响应中自现；推进规则属工程约定，已记于 `AGENTS.md`，故 README 与模板均不增补

## 4. 测试

- [x] 4.1 解析用例组：粘贴横幅（token + host 同时取到）、多地址私网优先（含 CGNAT 风格）、无地址行被忽略、空值不覆盖、手工 `host:` 行覆盖粘贴值、既有 `key: value` 行为回归；验证：用例通过 —— 证据：提交 `f57e8dc`；`PastedBannerTest` 6 项 + `ValueSemanticsTest` 2 项，另含「调用示例行不得成为 host 来源」与「手工 host 保留主机名」
- [x] 4.2 客户端端到端用例：工具副本中放入「粘贴式」文档，`hello` 不经 `--host` 即成功；验证：用例通过 —— 证据：提交 `f57e8dc`；`PastedBannerDocTest` 2 项，走进程内临时端口实例，不依赖产品端口
- [x] 4.3 全量回归；验证：`python3 -m unittest discover -s tests` 全绿、退出码 0，用例总数不少于改动前 —— 证据：`Ran 69 tests in 29.9s · OK (skipped=2)`，退出码 0（改动前 59 项）。**实施插曲**：首次全量跑出现一次未复现的失败，定位为新用例的竞态——留痕由服务端线程写出、与响应完成无先后保证（hello 先发响应后写留痕），断言可能读到半截缓冲；已加 `wait_until` / `await_trace` 等待收尾行，修复后连续 8 次全绿，另记 §7.3

## 5. 实跑验证

- [x] 5.1 本地实跑：起服务并把**真实启动横幅**整段粘进工具副本的 `bridge.local.md`，`hello` / `exec` / `download` 均不经 `--host` 成功；验证：命令与输出留证 —— 证据：横幅（列出两个地址，前一个为 CGNAT/VPN 段）整段粘贴后，`hello`（退出码 0，返回 version 字段）、`exec pwd`（退出码 0，输出为 `--workdir` 指定目录）、`download sample.txt`（退出码 0，取回 15 字节且内容逐字节一致）三个子命令均未带 `--host` / `--token`。私网优先取值由 §4.1 的解析用例断言（该实跑环境两个地址均可达，不足以区分）
- [x] 5.2 被控机实跑：用真实被控机横幅粘贴成的文档从客户端侧直接调用（不经 `--host`）；验证：`hello` 退出码 0、字段与被控机一致 —— 证据：横幅整段粘贴进客户端侧 token 文档后，`hello` **不带任何参数**（无 `--host`、无 `--token`）→ 退出码 0，返回的 version / user / system / cwd / started_at 等字段与横幅一致；`exec`（`dir /b`）与 `download`（工作目录内文件）同样不经参数成功。补记：被控机离线期间曾以同一份文档调用，报错为「无法连接 <被控机 IP>:37777」（退出码 3）而非改动前的「未指定 host」（退出码 2），即那时已证 host 从横幅正确取出

## 6. 收尾

- [x] 6.1 分提交推送（实现、测试、文档各自成提交）；验证：`git log --oneline` 与远端一致、`git status` 干净 —— 证据：提交 `2619db0`（实现）/ `f57e8dc`（测试）/ `8e866c4`（模板与 README）/ `6e68e67`（版本规则）；**已推送** `1122b61..6e68e67  main -> main`，`git status -sb` 与 `origin/main` 同步
- [x] 6.2 证据登记：勾选各项并附证据（提交哈希 / 命令 / 退出码 / 产物名）；未在目标平台实跑的不勾选 —— 证据：本次更新即登记（§5.2 于被控机恢复后补验通过，已勾选）
- [ ] 6.3 归档时按新规则 bump `BRIDGE_VERSION` 的 minor（具体数字以归档当时的值为准）；验证：`hello` 与横幅显示新版本号

## 7. 跟进项（本变更不实现，记录于此）

- [ ] 7.1 是否支持 IPv6 地址、或从横幅调用示例行提取地址——本变更只认「局域网 IP」行且只处理 IPv4，需要时另立变更
- [ ] 7.2 多私网网卡时如何自动选中可达地址（如按路由探测）——本变更取第一个私网地址，其余靠手工 `host:` 行覆盖，需要时另立变更
- [ ] 7.3 留痕的写出时机与响应完成之间**没有先后保证**：hello 先发响应、后打印响应小节；exec 的收尾行亦在终止 chunk 之后。规格未承诺该顺序（故本变更以测试侧等待收尾标记应对，见 §4.3），但若希望"响应返回即代表留痕已写完"，需改服务端写出顺序——另立变更
