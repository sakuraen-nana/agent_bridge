# agent-bridge 连接文档（模板）

复制本文件为同目录下的 `bridge.local.md` 并填写；正式文件已被 .gitignore 排除，
仅供本机使用。

## ⚠ 安全警示

- 此文件承载的 token 等价于被控机的完全控制权（可执行任意命令），泄露即机器失守。
- **不得将 `bridge.local.md` 加入任何同步渠道**（syncthing / 网盘 / 代码仓库 / 剪贴板同步工具等），
  也不得粘贴到日志、会话记录或聊天中。
- 不用时删除本文件即可（服务器侧 token 仅存内存，重启即失效）。

## 怎么填（两种写法，可混用）

**写法一（推荐）：把被控机控制台输出整段粘贴进来。**

服务器启动横幅里的这两行会被自动识别，无需任何手工改写：

```
  Token    : <这一行 → token>
  局域网 IP: <这一行 → host>
```

该行列出多个地址时（多网卡、含 VPN），取其中的私网地址（`10.` / `172.16-31.` / `192.168.`），
没有私网地址则取第一个。**要改指另一台机器，在文件末尾另写一行 `host: <目标 IP>` 即可覆盖**
（按行序，后出现者胜）。

**写法二：手工填写下面的字段。**

- `token:`（必填）——被控机启动 agent-bridge 服务器时控制台输出的随机 token。
  服务器**每次重启都会轮换** token，旧 token 立即失效；失效后请抄录最新值更新此字段。
- `host:`（选填）——被控机地址，供 client 缺省连接使用；不填则调用时须带 `--host` 参数。
  除 IP 外也接受主机名/域名。扫描（scan）不依赖此字段。
- `updated:`（选填）——最近一次填写日期，便于判断 token 是否可能已轮换。

> 两种写法可安全并存：**值为空的字段行不会覆盖已有取值**，因此下面留空的字段不会清掉你粘贴进来的内容。

## 字段

token:
host:
updated:
